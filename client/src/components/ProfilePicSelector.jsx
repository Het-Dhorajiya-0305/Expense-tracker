import React, { useRef } from 'react'

function ProfilePicSelector({image,setImage}) {
    const inputRef=useRef(null);
    const [preview,setPreview]=useState(null);
  return (
    <div>ProfilePicSelector</div>
  )
}

export default ProfilePicSelector